"use client";
import { FormEvent, useState } from "react";
import { ArrowUpRight, Check, WhatsApp } from "@/components/Icons";
import { contact } from "@/data/portfolio";
const types=[['Website','Site institucional'],['Dashboard','Dashboard'],['Web system','Sistema web'],['SaaS / digital product','SaaS & produtos digitais'],['Administrative system','Sistema administrativo'],['Automation / integration','Automação / integração'],['Other','Outro']];
export function EnglishBriefForm(){const [form,setForm]=useState({name:'',company:'',email:'',phone:'',type:types[0][1],details:'',privacy:false,website:''});const [status,setStatus]=useState<'idle'|'sending'|'ok'|'error'>('idle');const message=`Hello! I found NEXORA's portfolio and would like to discuss a project.\n\nName: ${form.name}\nCompany: ${form.company||'-'}\nContact: ${form.email||form.phone}\nProject: ${types.find(t=>t[1]===form.type)?.[0]}\n\nContext:\n${form.details||'-'}`;const whatsapp=`${contact.whatsappBase}?text=${encodeURIComponent(message)}`;async function submit(e:FormEvent){e.preventDefault();if(!form.privacy){setStatus('error');return}setStatus('sending');try{const response=await fetch('/api/leads',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name:form.name,company:form.company,email:form.email,phone:form.phone,projectType:form.type,features:[],budget:'Ainda não defini',timeline:'Quero conversar primeiro',details:form.details,privacyAccepted:true,website:form.website,source:'/en/contact'})});if(!response.ok)throw new Error();setStatus('ok')}catch{setStatus('error')}}return <form className="english-contact-form" onSubmit={submit}><input className="form-honeypot" tabIndex={-1} autoComplete="off" value={form.website} onChange={e=>setForm({...form,website:e.target.value})}/><div className="brief-form-grid"><label>Name<input required maxLength={80} value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label>Company <small>optional</small><input maxLength={100} value={form.company} onChange={e=>setForm({...form,company:e.target.value})}/></label><label>Email<input type="email" maxLength={180} value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label>WhatsApp<input maxLength={40} value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></label><label className="brief-details">Project type<select value={form.type} onChange={e=>setForm({...form,type:e.target.value})}>{types.map(([label,value])=><option key={value} value={value}>{label}</option>)}</select></label><label className="brief-details">Project context<textarea maxLength={3000} rows={7} value={form.details} onChange={e=>setForm({...form,details:e.target.value})} placeholder="What problem should the product solve?"/></label></div><label className="brief-privacy-check"><input type="checkbox" checked={form.privacy} onChange={e=>setForm({...form,privacy:e.target.checked})}/><span>I agree to the use of this information to respond to my request according to the <a href="/privacidade" target="_blank">privacy policy</a>.</span></label>{status==='ok'?<div className="form-success"><Check/> Request saved. <a href={whatsapp} target="_blank" rel="noreferrer">Continue on WhatsApp <ArrowUpRight size={14}/></a></div>:<button className="button button-primary" disabled={status==='sending'||(!form.email&&!form.phone)}>{status==='sending'?'Sending...':'Send project brief'}</button>}{status==='error'&&<p className="form-error">Please review the form and privacy consent.</p>}</form>}
