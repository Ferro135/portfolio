import type { Metadata } from "next";import Link from "next/link";import { Header } from "@/components/Header";import { ArrowRight } from "@/components/Icons";import { projects } from "@/data/portfolio";
export const metadata:Metadata={title:"Results",description:"Verified impact and project outcomes from NEXORA.",alternates:{canonical:"/en/results",languages:{'pt-BR':'/resultados','en':'/en/results'}}};
const impact=[['Zentra','Centralized operations','Clients, licenses, projects and operational states share the same management context.','zentra'],['Zentra','Less manual checking','Important status information is visible in the dashboard instead of spread across tools.','zentra'],['Spazio Management','Simpler daily routines','Sales, cash, inventory and pending items are easier to access from the main workflow.','spazio-management'],['Spazio Management','Clearer financial reading','Revenue, expenses and operational results use a direct visual hierarchy.','spazio-management']];
export default function ResultsEn(){return <main id="conteudo" lang="en"><Header/><section className="inner-page-hero"><div className="shell"><div className="inner-page-copy"><span className="eyebrow">Results</span><h1>Evidence before<br/>vanity metrics.</h1><p>NEXORA only publishes quantitative metrics when they are actually confirmed. Until then, case studies describe operational impact without invented numbers.</p></div></div></section><section className="section"><div className="shell results-impact-grid">{impact.map(([project,title,description,slug],i)=><article key={title}><span>0{i+1}</span><small>{project}</small><h3>{title}</h3><p>{description}</p><Link className="case-link" href={`/en/projects/${slug}`}>View case <ArrowRight size={15}/></Link></article>)}</div></section></main>}
