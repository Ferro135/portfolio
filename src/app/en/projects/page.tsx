import type { Metadata } from "next";import Link from "next/link";import { Header } from "@/components/Header";import { ProjectVisual } from "@/components/ProjectVisual";import { ArrowRight } from "@/components/Icons";import { projects } from "@/data/portfolio";import { listPublicCmsProjects } from "@/lib/server/business";import { safeHttpsUrl } from "@/lib/security";
export const metadata:Metadata={title:"Projects",description:"Selected NEXORA web products and systems.",alternates:{canonical:"/en/projects",languages:{'pt-BR':'/projetos','en':'/en/projects'}}};
const copy=[{href:'/en/projects/zentra',title:'Zentra',eyebrow:'SaaS · Management · Automation',description:'A SaaS platform created to centralize clients, licenses, projects and digital operations.'},{href:'/en/projects/spazio-management',title:'Spazio Management',eyebrow:'Finance · Operations · Dashboard',description:'An administrative system designed to simplify financial and operational management across multiple units.'}];
export default async function ProjectsEn(){const cms=await listPublicCmsProjects();return <main id="conteudo" lang="en"><Header/><section className="inner-page-hero"><div className="shell"><div className="inner-page-copy"><span className="eyebrow">Projects</span><h1>Real products.<br/>Different problems.</h1><p>Selected cases showing how interface, business rules and implementation work together.</p></div></div></section><section className="section"><div className="shell projects-grid">{projects.map((project,index)=><article className="project-card" key={project.id}><ProjectVisual project={project}/><div className="project-body"><span className="project-meta">{copy[index].eyebrow}</span><h2>{copy[index].title}</h2><p>{copy[index].description}</p><Link className="case-link" href={copy[index].href}>Explore case <ArrowRight size={15}/></Link></div></article>)}{cms.map(p=>{const image=safeHttpsUrl(p.image_url);return <article className="cms-project-card" key={p.id}>{image&&<img src={image} alt={p.title_en||p.title} loading="lazy" referrerPolicy="no-referrer"/>}<div><span className="project-meta">{p.eyebrow_en||p.category_en||p.eyebrow||p.category||"NEXORA project"}</span><h2>{p.title_en||p.title}</h2><p>{p.description_en||p.description}</p><Link className="case-link" href={`/en/projects/${p.slug}`}>View project <ArrowRight size={15}/></Link></div></article>})}</div></section></main>}
