import type { Metadata } from "next";import Link from "next/link";import { notFound } from "next/navigation";import { Header } from "@/components/Header";import { SiteFooter } from "@/components/SiteFooter";import { ArrowLeft, ArrowRight } from "@/components/Icons";import { getCmsProjectBySlug } from "@/lib/server/business";import { safeHttpsUrl } from "@/lib/security";
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const p=await getCmsProjectBySlug(slug);if(!p)return{};return{title:p.title_en||p.title,description:p.description_en||p.description,alternates:{canonical:`/en/projects/${p.slug}`,languages:{'pt-BR':`/projetos/${p.slug}`,en:`/en/projects/${p.slug}`}}}}
export default async function CmsProjectEn({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=await getCmsProjectBySlug(slug);if(!p)notFound();const title=p.title_en||p.title,description=p.description_en||p.description,longDescription=p.long_description_en||p.long_description||description,eyebrow=p.eyebrow_en||p.category_en||p.eyebrow||p.category||'ALUNERI project',image=safeHttpsUrl(p.image_url),demo=safeHttpsUrl(p.demo_url);return <main id="conteudo" lang="en"><Header/><section className="case-hero"><div className="shell"><Link className="case-back" href="/en/projects"><ArrowLeft/> Back to projects</Link><div className="case-hero-grid"><div><span className="eyebrow">{eyebrow}</span><h1>{title}</h1><p>{description}</p><div className="case-tags">{p.tags.map(tag=><span key={tag}>{tag}</span>)}</div>{demo&&<div className="case-hero-actions"><a className="button button-primary" href={demo} target="_blank" rel="noreferrer">Open demo <ArrowRight/></a></div>}</div>{image&&<div className="cms-case-cover"><img src={image} alt={title} referrerPolicy="no-referrer"/></div>}</div></div></section><section className="case-section"><div className="shell case-story-grid"><div className="case-story-title"><span className="eyebrow">Project</span><h2>Context and<br/>delivery.</h2></div><div className="case-story-content"><div className="story-block"><span>01</span><div><h3>The product</h3><p>{longDescription}</p></div></div>{p.metrics?.length>0&&<div className="story-block"><span>02</span><div><h3>Verified results</h3><div className="cms-metrics">{p.metrics.map((m,index)=><div key={`${m.label}-${index}`}><strong>{m.value}</strong><span>{m.label}</span><p>{m.description||''}</p></div>)}</div></div></div>}</div></div></section><SiteFooter locale="en"/></main>}
